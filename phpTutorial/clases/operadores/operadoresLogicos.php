<?php 
    header('Content-type: text/plain');  // Mostrar \n

    // OPERADORES LOGICOS

    // Operador AND

    $bool = (15 == 15) && (15 == 15);
    var_dump($bool); 

    /*
        AND --> && o 'and'
        OR --> || o 'or'
        XOR --> 'xor'
        NOT --> !

    */

?>